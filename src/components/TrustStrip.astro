---
const items = [
  { label: "SHA & NHIF accepted", detail: "National cover honoured at every branch" },
  { label: "M-Pesa payments", detail: "Pay consultation fees by till, no cash needed" },
  { label: "Private insurance", detail: "AAR, Jubilee, CIC, Britam & Madison" },
  { label: "Same-week appointments", detail: "Book via WhatsApp, call, or online form" },
];
---

<div class="border-y border-line bg-paper-2">
  <div class="mx-auto grid max-w-8xl grid-cols-2 gap-x-6 gap-y-6 px-6 py-8 md:grid-cols-4 md:px-10" data-reveal-group>
    {
      items.map((item) => (
        <div class="border-l-2 border-clay pl-4">
          <p class="text-sm font-semibold text-ink">{item.label}</p>
          <p class="mt-1 text-xs text-ink-2/60">{item.detail}</p>
        </div>
      ))
    }
  </div>
</div>
